# Nik Studio - core requirements
# Install with:  pip install -r requirements.txt

PySide6>=6.6
Pillow>=10.0

# Ships a working ffmpeg binary, which the video stages need.
# This avoids installing FFmpeg system wide - no admin rights, no PATH
# editing, and no winget (which older Windows 10 machines do not have).
# A system FFmpeg is used in preference to this one when there is one.
imageio-ffmpeg>=0.5

# ----------------------------------------------------------------------
# Optional: local GPU image generation (LocalBackend).
# These are large downloads and need an NVIDIA GPU, so they are NOT
# installed by default. Nik Studio runs fine without them - the Colab
# backend does the generating instead.
#
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install diffusers transformers accelerate safetensors
# ----------------------------------------------------------------------
